<h1>Name: <%= @dog.name %> </h1>
<p>Age: <%= @dog.age %> </p>
<p> <%= pluralize(@dog.activities.count, "activity" )%> </p>
<ol>Activity: <% @dog.activities.each do |activity| %> 
    <li><%= link_to activity.name, activity_path(activity) %> </li>
   
<% end %><br>
 
</ol>

<%= button_to "Delete Myself", @activity, method: :delete %><br>
<%= button_to "Home", dogs_path, method: :get %><br>
   
